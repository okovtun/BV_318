type name(parameters)
{
	.....;
	group-of-statements;
	.....;
	return value;
	return;
}

type - тип возвращаемого значения функции.
void - пустота.

name - имя функции.

parameters - список принимаемых параметров функции.

Global scope

type Function(int arr);		//Функция принимает переменную типа 'int'
type Function(int arr[]);	//Функция принимает массив типа 'int'

void FillRand(int arr[], const int n);